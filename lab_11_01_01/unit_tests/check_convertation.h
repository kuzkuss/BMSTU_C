#ifndef CHECK_CONVERTATION_H
#define CHECK_CONVERTATION_H

#include <check.h>

void convertation(unsigned short num, char *hex_num);

Suite* convertation_suite(void);

#endif // CHECK_CONVERTATION_H
